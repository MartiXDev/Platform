# Feature Management Generated Solution routing

This fixture is the isolated API acceptance seam for Microsoft Feature
Management. Keep provider registration direct, feature evaluation
fail-closed, telemetry bounded, and authorization and durable state independent
of feature flags.

Do not add secrets, Supported Capability claims, ASP.NET integration packages,
or `martix.agent.json`.
