# RepositoryBootstrapGeneratedSolution

This is the temporary, explicitly named Generated Solution seam for Repository
Bootstrap. It proves that a generated consumer has a routed agent entry point
and a secret-free `martix.platform.json` manifest.

It is test-owned scaffolding, not a product template and not evidence for a
Supported Capability claim.
