# Generated Solution Agent Routing

This fixture is intentionally limited to the Repository Bootstrap seam. The
canonical repository authorities are linked from the root `AGENTS.md`.

- Manifest: `martix.platform.json`
- Verification: `eng/verify.mjs` from the repository root
- No secrets or support claims belong in this fixture.
