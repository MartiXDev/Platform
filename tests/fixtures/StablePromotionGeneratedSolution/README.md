# Stable Promotion Generated Solution

`stable-promotion.json` is the immutable, claim-free Release Evidence index
for Stable Platform `1.0.0`. It promotes the accepted `1.0.0-rc.1` bytes
without rebuilding them and establishes the first major-floor compatibility
baseline.

Run the repository-owned verification surface with:

```text
npm run verify:stable-promotion
```
