# Deployment Manifest Generated Solution

This temporary Generated Solution is the primary acceptance seam for issue 35.
Its `deployment-manifest.json` records one validated topology and equal
process/OCI artifact identities. The accompanying evidence proves readiness,
liveness, graceful shutdown, external configuration, Migrator ordering,
promotion without rebuild, rollback, and projection drift rejection.

The fixture is claim-free. It does not contain environment values, credentials,
production build steps, cloud infrastructure, or an availability guarantee.
