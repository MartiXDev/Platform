# Deployment Manifest Generated Solution context

This named fixture proves that process archives and OCI images can be governed
by one normalized topology model. It is not a production application and makes
no cloud, orchestrator, high-availability, or Supported deployment claim.

Sensitive configuration is represented only by an external delivery contract.
The Migrator is a separate one-shot resource and serving resources become ready
only after its completed-work condition is satisfied.
